[package]
name = "kodework-storage"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
kodework-domain = { path = "../kodework-domain" }
rusqlite = { version = "0.40", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
uuid = "1"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
